// Este arquivo não está funcional, uso apenas para acompanhamento da aula,
// porque o url_launcher já é compatível com Web.

/*
import 'package:demo/demo.dart';
import 'plugin url_launcher' as url_launcher;

class AppInterface implements DemoInterface {
  @override
  launch(String url) {
    url_launcher.launch(url)
  }

}
 */
